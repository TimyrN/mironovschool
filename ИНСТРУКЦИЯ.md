# Инструкция по обновлению сайта

## Быстрая шпаргалка (3 команды)

Откройте терминал в папке проекта и выполните:

```powershell
git add .
git commit -m "Описание изменений"
git push origin master
npx vercel --prod
```

---

## Подробная инструкция

### Шаг 1: Откройте терминал в папке проекта

Путь к проекту:
```
c:\Users\79992\Desktop\сайты\таплинк_Артём\исходник\таплинк
```

Можно открыть папку в проводнике, зажать **Shift**, кликнуть правой кнопкой мыши и выбрать "Открыть окно PowerShell здесь".

---

### Шаг 2: Добавьте изменения в Git

Эта команда добавляет ВСЕ изменённые файлы:
```powershell
git add .
```

Если хотите добавить только конкретный файл:
```powershell
git add путь/к/файлу.jpg
```

---

### Шаг 3: Создайте коммит (сохранение)

```powershell
git commit -m "Краткое описание изменений"
```

Примеры:
- `git commit -m "Обновил фото профиля"`
- `git commit -m "Исправил текст на главной"`
- `git commit -m "Добавил новую ссылку"`

---

### Шаг 4: Отправьте изменения на GitHub

```powershell
git push origin master
```

После этого изменения появятся в репозитории:
https://github.com/TimyrN/mironovschool

---

### Шаг 5: Разверните на Vercel (обновите сайт)

```powershell
npx vercel --prod
```

Дождитесь сообщения:
```
✅  Production: https://...
🔗  Aliased: https://www.mironovschool.site
```

Готово! Сайт обновлён.

---

## Полезные команды

| Команда | Описание |
|---------|----------|
| `git status` | Посмотреть, какие файлы изменены |
| `git log -3` | Посмотреть последние 3 коммита |
| `git diff` | Посмотреть изменения в файлах |
| `npm run dev` | Запустить локальный сервер для тестирования |

---

## Если что-то пошло не так

### Ошибка "nothing to commit"
Значит, вы не изменили файлы или уже сделали коммит. Проверьте командой `git status`.

### Ошибка при push
Попробуйте сначала получить изменения:
```powershell
git pull origin master
```
Затем снова `git push origin master`.

### Vercel не обновляет сайт
Попробуйте принудительный деплой:
```powershell
npx vercel --prod --force
```

---

## Ссылки

- **Сайт**: https://www.mironovschool.site
- **Vercel Dashboard**: https://vercel.com/tims-projects-d711fa71/mironovschool
- **GitHub**: https://github.com/TimyrN/mironovschool
